function changeUser(element) {
  element.innerText = "Jason Y";
}

function removeFromList(element) {
  document.getElementById(element.remove());
}

function plusConnections(element) {
  element.innerText++;
}

function minusConnections(element) {
  element.innerText--;
}